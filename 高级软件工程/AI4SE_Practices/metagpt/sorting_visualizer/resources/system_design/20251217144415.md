## Implementation approach

我们将构建一个纯客户端的、基于Web的排序算法可视化工具，无需后端。核心是使用原生JavaScript实现排序算法，并在执行过程中暂停以记录每一步的状态（包括数组状态、高亮元素、统计信息）。然后利用HTML5 Canvas API将这些状态渲染为动态的条形图动画。UI将采用现代CSS（如Flexbox/Grid）实现响应式布局。为了代码清晰和可维护性，我们将采用模块化设计，将算法、可视化引擎、控制器和数据管理分离。我们选择Canvas而非SVG是因为它更适合需要频繁重绘的动画场景。考虑到易用性和性能，我们不依赖大型UI框架，仅使用原生JS和CSS。

## File list

- index.html
- css/style.css
- js/main.js
- js/algorithms/SortingAlgorithm.js
- js/algorithms/BubbleSort.js
- js/algorithms/SelectionSort.js
- js/algorithms/InsertionSort.js
- js/algorithms/QuickSort.js
- js/algorithms/MergeSort.js
- js/visualizer/Visualizer.js
- js/visualizer/ArrayData.js
- js/controller/PlaybackController.js
- js/controller/UIController.js
- js/utils/EventEmitter.js

## Data structures and interfaces


classDiagram
    class EventEmitter {
        +on(event: string, callback: Function)
        +emit(event: string, ...args)
    }

    class ArrayData {
        -array: number[]
        -size: number
        -state: string
        +generateRandom(size: number)
        +generateReversed(size: number)
        +setCustomArray(arr: number[])
        +getArray(): number[]
        +getSize(): number
        +swap(i: number, j: number)
        +setState(state: string)
        +getState(): string
    }
    EventEmitter <|-- ArrayData

    class SortingAlgorithm {
        <<abstract>>
        #data: ArrayData
        -steps: object[]
        -comparisons: number
        -swaps: number
        +sort(data: ArrayData) object[]
        #step(description: string, highlights: object[])
        #compare(i: number, j: number) boolean
        #swap(i: number, j: number)
    }
    EventEmitter <|-- SortingAlgorithm

    class BubbleSort {
        +sort(data: ArrayData) object[]
    }
    class SelectionSort {
        +sort(data: ArrayData) object[]
    }
    class InsertionSort {
        +sort(data: ArrayData) object[]
    }
    class QuickSort {
        +sort(data: ArrayData) object[]
        -quickSort(low: number, high: number)
        -partition(low: number, high: number) number
    }
    class MergeSort {
        +sort(data: ArrayData) object[]
        -mergeSort(low: number, high: number)
        -merge(low: number, mid: number, high: number)
    }
    SortingAlgorithm <|-- BubbleSort
    SortingAlgorithm <|-- SelectionSort
    SortingAlgorithm <|-- InsertionSort
    SortingAlgorithm <|-- QuickSort
    SortingAlgorithm <|-- MergeSort

    class Visualizer {
        -canvas: HTMLCanvasElement
        -ctx: CanvasRenderingContext2D
        -barWidth: number
        -maxBarHeight: number
        -colors: object
        +render(array: number[], highlights: object[], stats: object)
        +clear()
        -drawBar(x: number, value: number, color: string)
        -drawStats(stats: object)
    }

    class PlaybackController {
        -steps: object[]
        -currentStep: number
        -speed: number
        -timerId: number | null
        -isPlaying: boolean
        +loadSteps(steps: object[])
        +play()
        +pause()
        +next()
        +prev()
        +reset()
        +setSpeed(speed: number)
    }
    EventEmitter <|-- PlaybackController

    class UIController {
        -algorithmSelect: HTMLSelectElement
        -sizeSlider: HTMLInputElement
        -generateButtons: NodeList
        -playbackButtons: NodeList
        -speedSlider: HTMLInputElement
        -infoPanel: HTMLElement
        +bindEvents()
        -onAlgorithmChange()
        -onSizeChange()
        -onGenerateClick(type: string)
        -onPlaybackClick(action: string)
        -onSpeedChange()
        -updateInfoPanel(step: object)
    }

    class Main {
        -arrayData: ArrayData
        -currentAlgorithm: SortingAlgorithm
        -visualizer: Visualizer
        -playbackController: PlaybackController
        -uiController: UIController
        +init()
        -runVisualization()
    }
    Main --> ArrayData
    Main --> SortingAlgorithm
    Main --> Visualizer
    Main --> PlaybackController
    Main --> UIController
    PlaybackController --> Visualizer
    UIController --> Main


## Program call flow


sequenceDiagram
    participant U as User
    participant UI as UIController
    participant M as Main
    participant AD as ArrayData
    participant SA as SortingAlgorithm
    participant PC as PlaybackController
    participant V as Visualizer

    Note over U, V: 页面初始化
    M->>M: init()
    M->>AD: new ArrayData()
    M->>AD: generateRandom(默认大小)
    M->>V: new Visualizer(canvas)
    M->>PC: new PlaybackController()
    M->>UI: new UIController()
    UI->>UI: bindEvents()
    M->>V: render(初始数组, [], 初始统计)

    Note over U, V: 用户选择算法并点击“开始”
    U->>UI: 从下拉菜单选择算法
    UI->>M: onAlgorithmChange()
    M->>M: 根据选择实例化对应SortingAlgorithm子类
    U->>UI: 点击“开始”按钮
    UI->>M: onPlaybackClick('start')
    M->>M: runVisualization()
    M->>SA: sort(AD)
    SA->>AD: getArray()
    SA->>SA: 执行排序逻辑，通过step()记录每一步
    SA-->>M: 返回steps数组
    M->>PC: loadSteps(steps)
    PC->>PC: 重置状态
    PC->>PC: play()
    loop 动画循环
        PC->>PC: 获取当前step
        PC->>V: render(step.array, step.highlights, step.stats)
        PC->>UI: updateInfoPanel(step)
        PC->>PC: 等待speed毫秒后更新currentStep
    end

    Note over U, V: 用户交互（例如暂停、调速）
    U->>UI: 点击“暂停”
    UI->>PC: pause()
    PC->>PC: clearTimeout(timerId)
    U->>UI: 拖动速度滑块
    UI->>PC: setSpeed(newSpeed)

    Note over U, V: 用户生成新数据
    U->>UI: 点击“随机生成”
    UI->>AD: generateRandom(新大小)
    UI->>M: 触发重置流程
    M->>PC: reset()
    M->>V: render(新数组, [], 新统计)


## Anything UNCLEAR

需求中未明确说明是否需要实现‘步进’（单步前进/后退）功能的具体交互细节。在我们的设计中，PlaybackController已支持next()和prev()方法，UIController将暴露对应的按钮。另外，关于‘自定义输入数据’的交互形式（如文本框、拖拽）未详细说明，我们将在UI设计中采用一个简单的文本输入框，允许用户输入逗号分隔的数字。

