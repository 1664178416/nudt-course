利用 metagpt 实现的项目，提示词如下：

1. 一个排序算法可视化网页
2. 一个网页版2048小游戏，支持鼠标触控和键盘快捷键
3. 一个简单的多智能体框架（python 语言），使用 openapi 接口调用llm，支持自定义智能体

分别编号为`metagpt-1`、`metagpt-2`、`metagpt-3`，对应的项目代码分别在`sorting_visualizer`、`simple_agent_framework`目录下。其中，项目 2、3 的日志已保存在 log 文件中。

